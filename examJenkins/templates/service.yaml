apiVersion: v1
kind: Service
metadata:
  name: {{ include "examJenkins.fullname" . }}
  labels:
    {{- include "examJenkins.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "examJenkins.selectorLabels" . | nindent 4 }}
